
domain(algebra,

  system(Group,default,
         ruleset( 
             import(Monoid),
             $x*inv($x) -> One,
             inv(x)*$x -> One
                ),
         FirstTop)

);
       